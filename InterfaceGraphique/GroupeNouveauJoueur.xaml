<UserControl x:Class="InterfaceGraphique.GroupeNouveauJoueur"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
        <ComboBox x:Name="typeJoueur" Width="70" Margin="0,0,5,0">
            <ComboBoxItem Content="Concret" IsSelected="True"/>
            <ComboBoxItem Content="Computer"/>
        </ComboBox>
        <TextBox x:Name="nomJoueur" Width="120" Text="Joueur 1" Margin="0,0,5,0"/>
        <ComboBox x:Name="couleurJoueur" Width="50" Margin="0,0,5,0">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Fill="{Binding Name}" Width="16" Height="16" Margin="0,2,5,2" />
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <ComboBox x:Name="peupleJoueur" Width="100">
            <ComboBoxItem Content="Gaulois" IsSelected="True"/>
            <ComboBoxItem Content="Nain"/>
            <ComboBoxItem Content="Viking"/>
        </ComboBox>
    </StackPanel>
</UserControl>
